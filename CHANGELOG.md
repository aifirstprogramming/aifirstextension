## 1.6.0 - 2026-08-09

Sync content to v1.4.0 (48 -> 148 prompts). 29 carried, 8 changed, 111 added, 11 retired.

- Changed: Write intro code using both a short data type and an int data type.
- Changed: Write a simple Welcome {Name} program in Java that accepts the user's name as input.
- Changed: Change the code to use a do-while loop instead of a while loop.
- Changed: Update the for loop to decrement the lap instead.
- Changed: Write an intro Java pizza order program that offers 3 kinds of pizza, a user chooses which pizza, and the order is displayed back.
- Changed: Write a 12 line program with nested ifs to decide what dessert you should have based on whether it is hot outside and whether you like chocolate.
- Changed: Write a program that says hello three times.
- Changed: Write an 8 line program where you keep feeding the parrot seeds until it is full.
- Retired: Write intro code using a boolean data type without using logic like an if statement.
- Retired: Use a ternary operator to print a game level as novice, intermediate, or expert based on a number variable.
- Retired: Write a program that adds 3 numbers and prints the result
- Retired: Write a program that adds the average weight of an apple and an orange in pounds
- Retired: I have three items in my shopping cart. Use string concatenation to list them.
- Retired: Convert the string "i lOvE pUpPiEs" to uppercase
- Retired: Print the price of a specified item using an f string
- Retired: Print the price of a specified item using string concatenation
- Retired: Write a simple Boolean statement about the weather and print it
- Retired: Ask for a pet's type and name and output it
- Retired: Write a short program that counts from 5 to 1.
- Added: Create an intro Java example for serving 2 cake slices per person using mathematical operators for division and modulus.
- Added: Rewrite this code to use an import statement for Scanner rather than the full package within the code.
- Added: Write intro code using a boolean data type without conditional logic.
- Added: Could you write an intro function example?
- Added: Could you create a relatable intro Java example that shows how the four function modifiers work? Please start from widest access to narrowest access in your examples.
- Added: Write an intro Java program for basic houseplant care with functions using different return types and avoiding parameters.
- Added: Please update the code to one grow plant function that passes sunlight and water in as parameters.
- Added: Write an intro Java program that uses a function to convert Celsius to Fahrenheit for a travel agency application.
- Added: Please update this example to accept user input and do intro error handling with one try catch block.
- Added: Please update this example to also catch a NumberFormatException.
- Added: Please update this example to intro input validation that accepts a value between -273.15 and 100.
- Added: Please update this code to include a finally block.
- Added: Write an intro Java program that uses a function with arguments to make coffee. Add some magic as extra business logic in the function.
- Added: Please update this code to throw a particular error.
- Added: Could we update this to use a fun custom exception?
- Added: Please create a House class with the following properties: address, bedrooms, and bathrooms (double).
- Added: Please create a few sample house objects from the House.java class for Harry Potter's Privet Drive, Sherlock Holmes' Baker Street, and Friends' Bedford Street.
- Added: Please update the House class to include a toString method that returns a string representation of the house object.
- Added: Could you create a domain class for house rooms with name, length, and width fields?
- Added: Please write an intro example to create a few objects using the no-argument constructor of the Room.java domain class.
- Added: Could you update the HouseBuilder class to let builders input house layouts with how many rooms, use, and size? And then print the house info?
- Added: Please create an intro example with Java class for a cookie with a flavor field and a describe method.
- Added: Make a class for a decorated cookie that extends Cookie.java. Add a contructor and a override the describe method.
- Added: Please create a few sample cookie objects from the Cookie and DecoratedCookie classes and call their describe methods.
- Added: Please create example GiftCookie code that extends Cookie and has simple methods for packaging cookies.
- Added: Let's update the CookieBakery application for an intro business app, including use of Cookie, DecoratedCookie, and GiftCookie.
- Added: Create a Java class with a currentTemp variable and a method that returns "Heating", "Cooling", or "Idle" at certain temperatures.
- Added: Please write an intro app that uses the Thermostat class to create an object and print the status.
- Added: Would you create an intro JUnit test for Thermostat.java that checks that status is heating when the temperature is low?
- Added: Please create a Restaurant class with a method to get the spice level for a food that accepts an int for the level. Levels can be 1-5 with 1 mild and 5 spicy. Add unit tests, as well.
- Added: Could you add intro code for a new feature of eco mode to reduce the energy consumption by widening temperature thresholds?
- Added: Please update with an intro JUnit test to check that eco mode allows cooler temperatures before heating.
- Added: Could you please add a void printStatusToDisplay method that prints the thermostat status?
- Added: Could you add a test for the new printStatusToDisplay() method?
- Added: Please modify the printStatusToDisplay method to return a String instead.
- Added: Please write a intro tests for each thermostat status (regular and eco mode).
- Added: Would you please create a few bad unit tests for a traffic light example?
- Added: Could you update the tests and fix them?
- Added: Could we modify this with intro code to read temperature from a sensor?
- Added: Expand our Restaurant example for placing food orders. Orders should only be allowed at 11am or after, and tests should mock the current time.
- Added: Could you add code to display how many books are in the list, to get just the first book by its position, and try to add the same title twice.
- Added: Now could you update the Library to use List<Book> rather than List<String>?
- Added: Could you add examples to show removing an item, seeing if the list contains a certain book, and checking if the list is empty?
- Added: Please add another list to find all Fantasy books from our collection.
- Added: Create a Java PackingList class that uses a List to for String packing items with a duplicate item. Should add an item, remove an item by name, and print the full list with each item numbered without defining methods.
- Added: Could you add Eragon and Fable to the books collection, then create a List<String> genres variable to store the book genres and print the genres?
- Added: Could you update the library to use a Set collection for the genres list?
- Added: Create a Java RSVP class that uses a TreeSet to store rsvp names as Strings. Should add names to list (include duplicate), print the guest count, and print guests in alphabetical order.
- Added: Could you create a Map<String,String> bookAuthors variable to look up an author by book title?
- Added: Could you update the library to use a Map<String, Book> collection for the book authors list?
- Added: Create a Java program that uses a Map<String, String> as a glossary. Add the Java terms for List, Set, and Map along with their definitions. Print a specific definition by looking it up, and print all terms alphabetically.
- Added: Please create a subfolder with a small Maven Java app that includes JUnit as a dependency. Create two Java files: Song.java with fields for title, artist, genre, and energy level, and PlaylistApp.java with a main() method that stores Song objects in a list.
- Added: Sort the playlist by title alphabetically, by descending energy level, and then by artist.
- Added: Please add a code example for the song list as a HashMap (printing results), then as a TreeMap and printed.
- Added: Could you write an example to compare my playlist and a friend's playlist sets and find the intersection, union, and difference?
- Added: Using the Library app from chapter 7, create a new Java file for a book list with the following:
- Added: Please add an example for the playlist as a Queue and use some methods to process it.
- Added: Create a new file for a print queue with four print jobs. Check the queue before processing each job.
- Added: In the playlist app, please update songQueue to a Deque and use some Deque methods.
- Added: Create a Java program that uses an ArrayDeque<String> to simulate browser history. Add five page URLs. Simulate going back by polling from the front, then clear the two oldest entries from the back. Print the remaining history.
- Added: In the playlist app, please add code for the playlist as a PriorityQueue with only string titles. Process it with poll and display each one.
- Added: Please change the playlist app's PriorityQueue to Song objects compared by energyLevel. Process it with poll and display each one.
- Added: Add an example to the playlist app that uses a LinkedList for the playlist. Include how to add and remove a song from within the list.
- Added: Please create a subfolder with a small Maven Java app that includes JUnit as a dependency. Create two Java files: MarketProduct.java with fields for name, vendor, price (double), category, season, and organic (boolean). Also create a FarmersMarketApp.java with a main() method that stores various MarketProduct objects in a list.
- Added: Using the farmers market list, write a stream pipeline that finds all organic summer items, sorts them by price, extracts just their names, and collects the results into a List<String>.
- Added: Add a few more products to the list. Then write stream pipelines that: 1. extract vendors to a set, 2. display one vendor's menu of products and prices, 3. apply 10% discount to Baker's Lane products and collect new prices.
- Added: Please write two more streams that 1. Sum all prices using mapToDouble, and 2. Average price of organic items.
- Added: Create a new file for a weather app and a WeatherEntry class with a date (String), Celsius temperature, and condition. Add ten weather entries across different conditions and temperatures in the app, then write 3 stream pipelines that: 1. Find all rainy days, 2. Convert temperatures from Celsius to Fahrenheit, 3. Find average Celsius temperature for a week.
- Added: Could you please make the following changes to the FarmersMarketApp streams? 1. getVendorSet should be a list that is sorted alphabetically. 2. getVendorMenu needs to limit to 3 results.
- Added: Add a stream example for min and max that filters for a Craft category. Output 3 approaches to optional results (ifPresent, isPresent, orElse).
- Added: Create a new file for a sports team stats app and a Player class with a name (String), position (String, e.g. Forward, Midfielder, Defender, Goalkeeper), and goals scored (int). Add ten player entries across different positions in the app, then write 3 stream pipelines that: 1. Group players by position, 2. Find the top scorer overall, 3. Find the top scorer in each position.
- Added: Please take the getOrganicSummerItemNamesSortedByPrice stream and convert it to run in parallel. Are there any thread safety concerns with the current approach?
- Added: Ask for a pet's type and name, and output it
- Added: Write an eight line program to print attributes of a giraffe using basic data types.
- Added: Write a Python program that adds up the number of feet for a penguin, a panda, and a hippo.
- Added: Modify the program with a yes/no question to ask the user if they would like to use Celsius
- Added: Parrot needs 10 seeds to be full.
- Added: Write a short rocket program that counts from 5 to 1.
- Added: Modify the list to include a Portuguese item that would be easy to buy in a grocery store. Use changing, adding, removing, then accessing at the end.
- Added: Write an 7 line roll call program that goes through a fixed list of students, asks if they are present, and prints the list of absentees.
- Added: Create a program that creates a list of fish types with some duplicates and sorts them alphabetically
- Added: Using only tuples, describe a car with different attributes.
- Added: Write a program that uses tuple unpacking for Sam's winter gear. Unpack his clothes into three categories: a hat, outerwear, and shoes.
- Added: Create a dictionary for a fantasy world item shop containing a sword, a spell, and a potion, along with their price.
- Added: Create a dictionary for student classes at a magic academy. Create lists of classes for Sally and four other students.
- Added: Ask for user input to share cupcakes among llamas. Handle errors accordingly
- Added: Write a function to help Beepo the robot waiter roll up to the correct table and greet. The function should take one argument for the table number. Call the function three times.
- Added: Create a function to help Beepo the robot waiter to deliver food to a table. The function should take two arguments: a food list and table number. Call the function twice.
- Added: Create a function to help Beepo the robot waiter take payment from a numbered table according to the items ordered (everything costs $2). Call the function three times to collect money, put it in the cash register, and print the total.
- Added: Use this generator to play one hand of high card.
- Added: Create a list of goats and use filter to get the calmest ones.
- Added: Use reduce to calculate how many eggs a list of chickens laid in total. Then calculate how many muffins can be made from those eggs (1 egg per 6 muffins)
- Added: Create a robot pet class with the attributes name, battery, and mood. Then create three pets and print their attributes.
- Added: Add a method that checks if the pet has enough energy for a walk with a duration you can set and check the animals to see if they can do a 30 minute walk.
- Added: Use encapsulation to protect the battery attribute and ensure it doesn't go above 100 or below 0
- Added: Test walk and charge methods on Rover
- Added: Add robot pet subclasses for a cat, bird, and dog. Then create one of each type. Finally, call each animal’s method.
- Added: The talents should use the battery. Put this logic in the base class instead of performing a generic trick.
- Added: Create a RobotPetShelter class that stores robot pet objects in a list that is encapsulated. Add methods to add a pet, show all pets, and find a pet by name. Then create some pets.
- Added: Create a RobotPet class and a method that checks if the pet has enough battery to go on a walk.
- Added: Add two unit tests using assert to check if the pets can walk
- Added: Create a RobotPet class and a talent method
- Added: Write a unit test for the RobotPet class
- Added: Ran 4 tests in 0.000s
- Added: Create a battery for the robot pet and protect it so it does not go below 0 or above 100
- Added: Write a unit test to test the charge method
- Added: Create robot pet subclasses for a cat, bird, and dog.
- Added: Create unit tests for the battery and robot pet subclasses
- Added: RobotPets should say something different based on their type, battery, and mood.
- Added: Write unit tests to cover the new speak method
- Added: Expand the speak function to handle more cases

# Change Log

All notable changes to the "AI First Programming" extension will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.5.0] - 2026-08-08

### Changed
- Book content and prompt matching now come from the shared [`@aifirst/content`](https://github.com/aifirstprogramming/aifirstcontent) package instead of a copy inlined in this extension. The `aifirst` CLI already consumes the same package, so a prompt printed in the book resolves to the same code in the terminal and in VS Code — one implementation, two surfaces.
- **No behaviour change is intended.** The shared matcher was originally lifted verbatim from this extension. A golden regression test (`npm run test:unit`) replays 1,480 recorded cases — every authored prompt across every language scope, plus case, whitespace, `<prompt>`-tag and fuzzy variants — against the pre-migration results and requires them to be identical.

### Removed
- The bundled `book_content/` directory. The content repository is now the single source of truth; the books ship via the package's `books/*.json` subpath export.

## [1.4.0] - 2026-05-17

### Changed
- Renamed the language model from "AI First Book Examples" to "AI First Programming" everywhere it appears in the chat picker, walkthrough, and documentation. The model id is now `ai-first-programming` (previously `ai-first-book-examples`).

## [1.3.0] - 2026-05-16

### Added
- Full inline chat (`Ctrl+I` / `Cmd+I`) support — book examples now insert directly into your editor with the native Keep/X diff overlay, the same UX as Copilot's built-in models. Works for both saved files (uses Copilot's `replace_string_in_file` tool against the absolute path) and untitled buffers (inserts a placeholder and addresses the tool call against the `untitled:` URI form).
- Chat panel responses now render as fenced markdown code blocks with native **Insert at Cursor** / **Apply** actions instead of plain conversational text.

### Changed
- Advertised `toolCalling: true` capability so the model surfaces in the Copilot Chat picker (recent Copilot Chat builds filter out models without this).
- Increased `maxInputTokens` to 128000 and `maxOutputTokens` to 4096.
- Prompt matcher now strips the `<prompt>...</prompt>` wrapper Copilot Chat adds in inline chat, so fuzzy scoring sees the user's actual text.
- For plaintext / untitled buffers, the matcher exhausts Python entries entirely before falling through to Java, so a blank file defaults to Python examples.
- Documentation updated for the eye-icon UI in Manage Language Models (replaces the older checkbox) and for the new inline-chat workflow.

## [1.2.0] - 2025-12-06

### Fixed
- Fixed transparency on the icon

## [1.1.0] - 2025-12-06

### Added
- Added an icon for the plug-in

## [1.0.0] - 2025-12-06

### Added
- Initial release of AI First Programming extension
- Book content browser with hierarchical navigation (Books → Sections → Chapters → Examples)
- Example viewer with syntax highlighting using Prism.js
- Copy functionality for prompts and responses with visual feedback
- AI First Book Examples Language Model Chat Provider
- Language-aware prompt matching (filters by active editor language)
- Support for Python and Java book content
- Getting started walkthrough with 5 steps
- Sidebar tree view for browsing book content
- Webview panel for displaying example details
- Multiple matching strategies (exact, partial, fuzzy matching)
- Zero token cost when using AI First Book Examples model
- Support for both single prompt and multiple prompts per example

