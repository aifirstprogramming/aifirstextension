out
dist
node_modules
.vscode-test/
*.vsix

# Claude Code per-project settings
.claude/

# OS junk
.DS_Store
Thumbs.db

# npm/yarn logs
*.log
npm-debug.log*
yarn-error.log

# JetBrains
.idea/

# CI scratch from content-sync.yml (the workflow writes this to $RUNNER_TEMP now,
# but keep it ignored as a backstop against a future regression).
/delta.json
